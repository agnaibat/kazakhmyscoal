backend:
  name: git-gateway
  branch: main

media_folder: "img/uploads"
public_folder: "/img/uploads"

collections:
  - name: "news"
    label: "Новости"

    folder: "content/news"

    create: true

    slug: "{{slug}}"

    fields:
      - label: "Заголовок"
        name: "title"
        widget: "string"

      - label: "Дата"
        name: "date"
        widget: "datetime"

      - label: "Текст"
        name: "body"
        widget: "markdown"